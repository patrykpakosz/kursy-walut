name: Aktualizuj widget piłkarski
on:
  workflow_dispatch:
  schedule:
    - cron: "30 11 * * 1-5"
permissions:
  contents: write
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Informacja
        run: echo "Dane startowe są opublikowane; pełny updater zostanie dodany w kolejnej wersji."
